# uv override file — absolute pins that REPLACE whatever constituent packages
# declare. Passed as `uv pip install --overrides`.
#
# Unlike pip constraints (additive, so a declared `torch>=2.7` still wins and
# the resolution fails), uv overrides replace the declared requirement outright.
# That keeps the complete environment on one resolved CUDA/PyTorch stack.
#
# Values must stay in sync with vendor/lerobot-smolvla-cu128.yml.

# Compute stack: first stable Blackwell-capable PyTorch release.
torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128

# Benchmark/model semantics. Everything else is allowed to resolve forward.
numpy==2.2.6
scipy==1.18.0
transformers==5.5.4
hf-libero==0.1.4
robosuite==1.4.0
bddl==1.0.1
setuptools==80.10.2
fsspec==2026.2.0

# EVAL-COMPARABILITY pin, not a compatibility one: mujoco 3.8.1 renders darker
# floors and understates success rate, so anything measured under it is not
# comparable to the repo's numbers. robosuite/libero would otherwise pull a
# newer one. Asserted in the image's build-time smoke test.
mujoco==3.3.2
